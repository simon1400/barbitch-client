// Metadata pro tento krok rezervace drží `page.tsx` — dřív tu byla druhá,
// prakticky shodná kopie, kterou Next stejně přebíjel tou ze stránky.
export default function PersonalLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
